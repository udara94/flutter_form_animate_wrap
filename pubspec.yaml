name: form_animate_wrap
description: Animated wrappers which can used to wrap around TextFormField to get some nice interactions
version: 0.0.7
homepage: https://github.com/udara94/flutter_form_animate_wrap

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: